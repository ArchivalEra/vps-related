# T07: New single-input flow E2E acceptance matrix (task)

- **label**: `wayfinder:task`
- **type**: checklist first, resolve after main-body acceptance (AFK parts can be self-driven; the live-link verification needs the local test-env)
- **blocked by**: 002 (conversion coverage), 005 (address strategy), 006 (output scaffolding)
- **blocks**: —

## Question

`assert_gen` is a structure self-check (layer 0); it can't prove **real link connectivity**. The whole pipeline "real server config.json → `gen-client.sh --from-server` → client.json → `sing-box check` → live-link curl" needs to be turned into a regression matrix on `test-env/` as the final confirmation before the map's end. Once the prerequisites (002/005/006) are settled, the checklist includes:

1. **Server config supply**: the server config generated by test-env/setup.sh must cover the new single-input flow (multiple protocol inbounds in one config: reality/vless-ws/hy2/shadowtls+ss chain/tuic/anytls/ss/naive)
2. **E2E regression**: `--from-server server/config.json --server 127.0.0.1 --insecure` → output passes `sing-box check` → per-line socks5 + curl 204 live links (reusing the run-test.sh pattern), five lines 5/5 (wg removed)
3. **Abnormal-input matrix**: empty inbounds → 2, unsupported type → per 002's ruling (warn or 2), bad JSON / missing python3 / no-binary degradation path — assert the exit code for each
4. **Version-timeline scenarios**: drive `check_version` with fake version strings to assert the warnings and behavior for supported/deprecated_ok/future/unknown (per 003's ruling)
5. **Idempotency regression**: two runs on the same input produce identical md5
6. **Record**: paste the test results back into this ticket, as the acceptance evidence for the 002/005/006 decisions

## Why needed

The self-check proves "the structure is right"; the E2E matrix proves "it actually connects and upgrades don't break things". Both legs are required; this ticket is the final gate confirming the conversion pipeline is robust before the map's end.

## Update (2026-08-13, user addition)

- User request: **run two sing-box processes locally for real outbound/inbound live testing** (not just check) — confirm the converted output can actually proxy traffic.
- Minimal verification already done: six-line server on local 127.0.0.1 + converted client.json, curl through socks hitting gstatic 204 passed (HTTP 204).
- Full live testing to be completed (when executing this ticket): test **all** six lines through the converted output one by one, UDP traffic, multi-round/long-duration stability, and proxy large-file downloads for throughput.
- Prerequisites: this ticket is blocked by 002/005/006 (conversion coverage / address strategy / output scaffolding); the live tests should be done after those are settled.
